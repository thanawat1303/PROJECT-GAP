-- CREATE TABLE source_list (
--     id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
--     name varchar(80) NOT NULL,
--     is_use BOOLEAN NOT NULL,
--     location point
-- )

-- ALTER TABLE source_list
-- MODIFY location point

-- ALTER TABLE `source_list` CHANGE `is_use` `is_use` TINYINT(1) NOT NULL;